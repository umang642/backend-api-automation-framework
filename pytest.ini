[pytest]
addopts = -ra -q
testpaths = tests
markers =
    smoke: critical path checks
    contract: contract tests from OpenAPI
    perf: performance checks
    negative: negative/edge tests
filterwarnings =
    ignore::DeprecationWarning
