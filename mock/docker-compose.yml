services:
  mock:
    image: python:3.11-slim
    working_dir: /app
    volumes: [ ".:/app" ]
    command: bash -lc "pip install -r requirements.txt && uvicorn mock.server:app --host 0.0.0.0 --port 8000"
    ports: [ "8000:8000" ]
